let input = require('fs').readFileSync('/dev/stdin').toString().trim().split('dz=').join('a').split('lj').join('a').split('nj').join('a').split('-').join('').split('=').join('')

console.log(input.length)
